/** Cookie names shared by the server and the preview badge — no secrets here. */
export const PREVIEW_COOKIE = "zk_preview";
/** Readable by the page and carrying nothing secret; it only shows the preview badge. */
export const PREVIEW_FLAG_COOKIE = "zk_preview_on";
export const PREVIEW_HOURS = 12;
